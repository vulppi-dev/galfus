[package]
name = "vulfram-bindings-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
vulfram-core = { path = "../..", default-features = false, features = ["ffi"] }
