[package]
name = "galfus-bindings-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
galfus-core = { package = "galfus-runtime", path = "../galfus-runtime", default-features = false, features = ["ffi"] }
