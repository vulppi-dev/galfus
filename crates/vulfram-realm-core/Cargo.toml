[package]
name = "vulfram-realm-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
glam = { version = "0.30", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
vulfram-types = { path = "../vulfram-types" }
